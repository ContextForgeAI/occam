#!/usr/bin/env bash
# Unified FF-Occam operator CLI — delegates to occam.mjs
set -euo pipefail
ROOT="${OCCAM_HOME:-$(cd "$(dirname "$0")/.." && pwd)}"
export OCCAM_HOME="$ROOT"
exec node "$ROOT/scripts/occam.mjs" "$@"
