# HTTP Core Module

### client_max_body_size index

TOC_ENTRY

- [client_max_body_size](#client_max_body_size)
- [client_body_buffer_size](#client_body_buffer_size)
- [client_body_timeout](#client_body_timeout)

The index lists client_max_body_size repeatedly: client_max_body_size, client_max_body_size, client_max_body_size. Additional index prose discusses configuration contexts, directive syntax, modules, inheritance, examples, variables, phases, and request processing so that the document is larger than the constrained budget.

## Unrelated examples

Long introductory examples discuss server configuration, request routing, proxy settings, logging, cache behavior, and deployment. This padding intentionally consumes a constrained window before the definition.

## Request body limit {#client_max_body_size}

ANSWER_NGINX. Sets the maximum allowed size of the client request body. The default value is 1m.
