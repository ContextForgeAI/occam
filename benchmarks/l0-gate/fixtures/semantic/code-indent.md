# Indentation example

Python functions keep indentation. Call `greet("occam")` after import.

```python
def greet(name):
    if name:
        return f"hello {name}"
    return "hello"
```

The inner `return` must stay indented under `if name`.

## CSS noise

Stylesheet chrome that must not ride along with a focused Python answer.

```css
body { color: red; }
  .nav { display: none; }
```

Astronomers observe distant galaxies through enormous optical telescopes
and write observing proposals that never mention greet or indentation.
