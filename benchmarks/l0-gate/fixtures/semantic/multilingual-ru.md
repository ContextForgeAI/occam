# Установка

`occam connect`

После установки выполните:

1. Добавьте `occam` в PATH.
2. Запустите `occam connect` для выбранного хоста.
3. Не вызывайте `npx` как замену операторского CLI.

The greenhouse temperature must stay above 18 C.

## Gardening

Поливайте розы каждое утро.
