# Compare known sources

One `occam_digest` over two official nginx pages. Both URLs succeeded.
Focus was **weak / not found** for `proxy_read_timeout proxy_pass`, and the
host said so instead of presenting `combined` as a focused answer.

That honesty is the point of this recording: a useful excerpt can still
arrive with `focusMatched:false`. Follow `agentHints`.

- [result-summary.json](result-summary.json) · [success-output.txt](success-output.txt)
- [settings.json](settings.json) · [input-metadata.json](input-metadata.json)
