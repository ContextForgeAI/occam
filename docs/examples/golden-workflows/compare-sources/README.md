# Compare known sources

One `occam_digest` over two official nginx pages, captured on GitHub Release
host **v1.1.1**. Both URLs succeeded. The module page was `focusMatched:true`
with `focus:weak`; the beginner page stayed `focusMatched:false`. The host
did not present `combined` as a focused answer.

That honesty is the point of this recording: a useful excerpt can still
arrive with a weak or missing focus match. Follow `agentHints`.

- [result-summary.json](result-summary.json) · [success-output.txt](success-output.txt)
- [settings.json](settings.json) · [input-metadata.json](input-metadata.json)
