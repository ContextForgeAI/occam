# nginx proxy docs (bounded research)

Recorded `occam research` folder. Seed: `https://nginx.org/en/docs/`.
Focus: `proxy_pass proxy_read_timeout`. Stop: `budget_pages` after two
successful extracts. Off-site URL listed in `discovery.outOfScope`.

**Host:** `ff-occam/1.0.0-rc.2` assembled from golden excerpts — not a
GitHub Release `v1.0.0` certification.

Replay from [`_inputs/nginx-proxy.json`](../_inputs/nginx-proxy.json).
Parent: [site research](../).
